# 클레임 자동 분석 대시보드 UI 사양서

## 1. 목표

제조업 품질관리 담당자가 업로드된 클레임 DATA와 검수 DATA를 한 화면에서 빠르게 비교하는 프리미엄 제조 품질 대시보드.

데이터 로딩, 필터, 집계, PPM 계산 로직은 기존 구현을 그대로 유지하고 UI 표현·레이아웃·스타일만 변경한다.

## 2. 디자인 원칙

- 전체 인상: 신뢰감 있는 제조 품질관리 시스템
- 기본 배경: 아주 연한 블루 `#EEF6FF`
- 콘텐츠 카드: 흰색 `#FFFFFF`, 1px `#D9E7F5`, 14~18px radius
- 주 색상: 딥 네이비 `#082B52`
- 강조 색상: 블루 `#1677E8`, 밝은 블루 `#2F8CF3`
- 성공/증가: 그린 `#10B981`
- 경고: 앰버 `#F59E0B`
- PPM 라인: 레드 오렌지 `#F97316`
- 본문 폰트: Windows `Malgun Gothic`
- 숫자: 천단위 콤마, KPI는 굵은 대형 숫자

## 3. 화면 구조

### 3.1 좌측 사이드바

- 폭: 212~220px
- 배경: `#082B52`
- 상단: Wooshin 로고 및 회사명
- 제목: `클레임 자동 분석`
- 부제: `Claim Analytics`
- 선택 메뉴: 밝은 블루 그라데이션 또는 `#1677E8`, 흰색 글자
- 메뉴 항목: 클레임 분석, 고객사별 DATA 저장, DATA 업로드, 자동분석 현황, 상주분석 현황
- 하단: 차량/부품 이미지와 품질 슬로건

### 3.2 상단 필터 바

- 흰색 카드형 컨테이너
- 필터 순서: 데이터 원본, 고객사, 차종, 공정, 구분, 원인, 생산분석 구분
- 라벨은 입력창 위에 배치
- 입력창 높이 38~42px, radius 8px
- 분석 시작 버튼은 우측 고정, 파란색, 아이콘 포함
- 필터 변경 시 기존 분석 로직만 호출하고 레이아웃은 유지

### 3.3 KPI 카드

4개 카드를 한 줄에 배치한다.

1. 총 클레임
2. 총 발생건수
3. 총 생산건수
4. 발생률(PPM)

각 카드 구성:

- 좌측 58~68px 아이콘 박스
- 아이콘 박스: 블루 계열 gradient
- KPI 제목: 13~14px, 네이비
- KPI 숫자: 28~34px, 굵게
- 전월 대비 변화량: 그린 또는 레드, 작은 보조 문구

### 3.4 메인 분석 카드

- 카드 제목: 아이콘 + `클레임 분석`
- 탭: 월별, 분기별, 연도별
- 기본 탭: 월별
- 범례: 발생건수(파랑), 발생률(PPM)(주황/빨강)
- 차트 영역은 충분한 내부 여백과 넓은 가로 스크롤을 제공
- 월별 표는 그래프 아래에 배치하며 그래프와 동일한 DATA를 사용

### 3.5 월별 차트

- 발생건수: 파랑 막대 `#2563EB`
- 생산건수: 연한 회색 막대 `#CBD5E1`
- PPM: 보조축 선 그래프 `#F97316`
- 막대 상단 숫자 라벨 표시
- 가로 grid만 표시, 매우 연한 회색
- 상단·우측 테두리 제거
- 0값 라벨은 생략 가능
- 축 제목은 작고 명확하게 표시

### 3.6 하단 분석 카드

4개 카드를 동일 폭으로 배치한다.

- 현상별 분석
- 사용기간 분석
- 주행거리 분석
- 국가별 분석

각 카드:

- 상단 아이콘 + 제목 + `자세히 보기` 링크
- 동일한 파랑 막대 스타일
- 막대 상단 숫자 라벨
- 긴 항목명은 2줄 처리
- 카드 높이와 내부 여백 통일

## 4. 반응형/성능

- 최소 해상도: 1280×720
- 권장 해상도: 1600×900 이상
- 창 이동/리사이즈 시 렌더링 디바운스 적용
- 동일 DATA에 대한 집계 캐시 사용
- 콤보박스 선택 시 UI를 먼저 갱신하고 무거운 차트는 예약 렌더링
- 기존 DATA 및 분석 로직은 수정하지 않는다.

## 5. 품질 기준

- 모든 카드의 radius, padding, typography scale 통일
- 콘텐츠 정렬선은 좌우 24px 기준으로 맞춤
- 색상은 네이비·블루·화이트 중심으로 제한
- 아이콘은 동일한 선 굵기와 시각적 크기 유지
- 숫자 정렬은 우측 또는 중앙 정렬을 일관되게 사용
