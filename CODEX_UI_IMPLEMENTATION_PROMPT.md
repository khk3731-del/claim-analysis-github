# Codex 구현 프롬프트

아래 요구사항을 만족하도록 현재 Python Tkinter 클레임 자동분석 프로그램의 UI를 참고 이미지와 동일한 구조로 리디자인하라.

중요: 기존 Excel 불러오기, 데이터 저장/복원, 고객사·차종·품명·구분·품번 필터, 검수 DATA 연동, 조립수 집계, PPM 계산, 보고서 출력 로직은 절대 변경하지 말라. 변경 대상은 UI 레이아웃, 스타일, 그래프 표현뿐이다.

## 참고 화면

첨부된 제조업 품질 대시보드 이미지를 시각적 기준으로 사용한다. 화면은 왼쪽 네이비 사이드바와 오른쪽 연한 블루 작업영역으로 구성한다.

## 구현 요구사항

1. `claim_dashboard.py`를 기준으로 작업한다.
2. 기존 Tkinter 구조를 유지한다. 프레임워크를 교체하지 않는다.
3. Windows 폰트는 `Malgun Gothic`을 사용한다.
4. 전체 배경은 `#EEF6FF`, 사이드바는 `#082B52`, 카드 배경은 `#FFFFFF`로 한다.
5. 좌측 사이드바는 약 215px 폭으로 구성하고 회사 로고, 제목, 메뉴, 하단 이미지를 배치한다.
6. 상단 필터 영역은 흰색 카드로 만들고 입력창을 같은 높이와 radius로 통일한다.
7. KPI 카드는 4개를 한 줄로 배치하고 아이콘 박스, 제목, 큰 숫자, 변화율 영역을 갖는다.
8. 월별 메인 카드는 제목, 탭, 범례, 콤보 차트, 월별 표 순서로 구성한다.
9. 월별 그래프는 발생건수/생산건수를 막대로, PPM을 보조축 선으로 표현한다.
10. 그래프 색상은 발생건수 `#2563EB`, 생산건수 `#CBD5E1`, PPM `#F97316`을 사용한다.
11. 그래프 배경은 흰색, top/right 테두리는 제거, 가로 grid만 연하게 표시한다.
12. 모든 막대 위에는 천단위 콤마 숫자 라벨을 표시하고 0값 라벨은 생략한다.
13. 현상별, 사용기간, 주행거리, 국가별 그래프도 동일한 카드·색상·폰트·여백 시스템을 적용한다.
14. 공통 스타일 함수 `apply_modern_style(ax)` 또는 현재 Tkinter Canvas 구조에 대응하는 동등한 공통 스타일 함수를 만든다.
15. 창 크기 변경 시 전체 그래프를 매 이벤트마다 다시 그리지 말고 디바운스한다.
16. 동일한 DATA의 집계는 캐시한다.
17. 수정 후 기존 필터와 계산 결과가 변경되지 않았는지 확인한다.

## 검증 항목

- 프로그램이 정상 실행되는가
- DATA 업로드 및 저장/복원이 유지되는가
- 고객사·차종·품명·구분·품번 선택이 기존과 동일하게 동작하는가
- 월별 발생건수, 생산건수, 조립수, PPM 값이 변경되지 않는가
- 다른 모니터로 창을 이동할 때 화면이 깨지지 않는가
- 보고서 출력 기능이 기존과 동일하게 동작하는가
- 1280×720 이상에서 UI가 겹치지 않는가
